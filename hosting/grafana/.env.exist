# Grafana admin login. Seeded on first boot only — change it later in the UI, not here.
GRAFANA_ADMIN_USER=EXIST_USERNAME
# Admin password. Wrong value locks you out of every dashboard; anonymous access is off.
GRAFANA_ADMIN_PASSWORD=EXIST_PASSWORD
