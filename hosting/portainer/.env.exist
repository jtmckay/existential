# Portainer reads no per-service env vars at this layer — the admin password
# is loaded from ./portainer_password.txt at startup, and NFS settings
# come from .env.shared (EXIST_NFS_SERVER_ADDRESS / EXIST_NFS_BASE_PATH).
