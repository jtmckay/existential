# Portainer admin password — generated once by templates.sh, stored in .env.
# Passed to the container as PORTAINER_ADMIN_PASSWORD and written to a temp
# file at container startup (see command: in docker-compose.exist.yml).
PORTAINER_ADMIN_PASSWORD=EXIST_DEFAULT_PASSWORD
