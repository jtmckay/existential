# Portainer reads no per-service env vars at this layer — the admin password
# is loaded from ./portainer_password.txt at startup, and TrueNAS NFS settings
# come from .env.exist (EXIST_TRUENAS_SERVER_ADDRESS / EXIST_TRUENAS_CONTAINER_PATH).
