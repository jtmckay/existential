# Only the dependency manifests + entrypoint are needed to build the image.
# Exclude runtime state, secrets, and shared code (mounted at runtime, never baked).
*
!Dockerfile
!entrypoint.sh
!package.json
!pnpm-lock.yaml
!pnpm-workspace.yaml
