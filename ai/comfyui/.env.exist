# ComfyUI — node-based image/video generation UI
# Extra ComfyUI startup flags, passed through as CLI_ARGS (e.g. --lowvram on an
# 8 GB card). Blank is correct: the image already supplies --listen --port.
COMFYUI_ARGS=
