# OpenWebUI — Environment Variables

# Hermes gateway API key — matches EXIST_HERMES_API_KEY in .env.exist
OPEN_WEBUI_HERMES_API_KEY=EXIST_HERMES_API_KEY

# LightRAG API key — matches EXIST_LIGHTRAG_API_KEY; used for the second
# OpenAI-compatible connection so LightRAG models appear in the model picker.
OPEN_WEBUI_LIGHTRAG_API_KEY=EXIST_LIGHTRAG_API_KEY

# Admin account created on first boot
OPEN_WEBUI_ADMIN_NAME=EXIST_USERNAME
OPEN_WEBUI_ADMIN_EMAIL=EXIST_EMAIL
OPEN_WEBUI_ADMIN_PASSWORD=EXIST_PASSWORD

# JWT signing secret — keep separate from other keys
OPEN_WEBUI_SECRET_KEY=EXIST_32_CHAR_HEX_KEY

# Authelia OIDC (Phase 2) — OAUTH_*/OPENID_* are mapped in compose environment block.
# Redirect URI registered in Authelia: https://open-webui.<domain>/oauth/oidc/callback
# NODE_EXTRA_CA_CERTS is set directly in compose (no var needed — path is constant).
OPEN_WEBUI_ENABLE_OAUTH_SIGNUP=true
OPEN_WEBUI_OAUTH_CLIENT_ID=open-webui
OPEN_WEBUI_OAUTH_CLIENT_SECRET=EXIST_OIDC_OPEN_WEBUI_CLIENT_SECRET
OPEN_WEBUI_OPENID_PROVIDER_URL=https://authelia.EXIST_DOMAIN/.well-known/openid-configuration
OPEN_WEBUI_OAUTH_PROVIDER_NAME=Authelia
OPEN_WEBUI_OAUTH_SCOPES=openid email profile groups
