**/.env
**/.env.local
**/.env.generated
/.env.shared
/.claude/worktrees

# Temp files created by existential.sh and e2e tests (always inside the repo)
/.tmp.*
/.tmp-e2e-*

# Per-service "exist.initial.sh has run" sentinel
**/.existential.initialized
**/.pnpm-store
.claude/settings.local.json

**/minio_data
**/n8n_data
**/nextcloud_data
**/openwebui_data
**/nextcloudsql_data
**/ollama_data
**/pg_data
**/qdrant_data
**/redis_data
**/speaches_data
**/uptime-kuma_data

**/node_modules

**/docker-compose.yml
/docker-compose-*.yml

# Bridge Config
**/bridge.config.json

# Chatterbox
**/chatterbox/config.yaml
**/chatterbox/logs
**/chatterbox/outputs

# Authelia (rendered config + the generated user DB with argon2 hashes)
**/authelia/configuration.yml
**/authelia/users_database.yml

# Caddy
**/Caddyfile

# Caddy TLS certs (hosting/caddy/certs/) — only the *.exist.pem templates are tracked.
# Cloudflare origin pair (user-supplied):
**/cloudflare-key.pem
**/cloudflare.pem
# Stable *.internal cert + CA, minted by caddy/exist.initial.sh (keys are secret;
# certs are host-local generated artifacts — none are committed):
**/internal-ca-key.pem
**/internal-ca.pem
**/internal-key.pem
**/internal.pem
**/internal.csr
**/internal-ca.srl

# Code-server
**/opencode.json
workspace

# Dashy
**/dashy-conf.yml

# Decree
**/decree/webhook/config.yml
automations/lib/file-processors/*
automations/runs/*
automations/secrets/*
# Per-decree-daemon runtime state (main decree + all per-service sidecars)
**/decree/config.yml
**/decree/config.hash
**/decree/cron/*
**/decree/data/*
**/decree/emails/
**/decree/inbox/*
**/decree/outbox/
**/decree/processed.md
# Sidecar nested bind-mount points — dirs tracked (.gitkeep force-added), content ignored.
# These must exist before first `docker compose up` so Docker doesn't auto-create them as root.
**/decree/shared_routines/*
**/decree/lib/*
**/decree/runs/*

# Hermes
**/hermes/data

# LightRAG
**/lightrag/rag_storage

# Immich
**/immich/library
**/immich/postgres

# LibreChat (graveyard)
**/librechat/data-node
**/librechat/logs
**/librechat/meili_data_v1.12
**/librechat/uploads
**/librechat/librechat.yaml

# Nextcloud
**/nextcloud/servername.conf

# NTFY
**/ntfy-config/server.yml
**/ntfy-config/user.db
**/mqtfy-config.yml

# Ollama
**/ollama/entrypoint.sh
**/ollama/decree/migrations/*

# Pi-Hole
**/piHole/etc-pihole

# Playwright
**/mcp/playwright-data
node_modules/
/automations/playwright/test-results/
/automations/playwright/playwright-report/
/automations/playwright/blob-report/
/automations/playwright/playwright/.cache/

# Portainer
**/portainer/portainer_password*

# RabbitMQ
**/ssl/ca.pem
**/ssl/client*
**/ssl/server_cert.pem
**/ssl/server_key.pem
**/rabbitMQ/defs.json

# StirlingPDF
**/StirlingPDF

# VLLM
**/vllm/.env

# Windmill
**/windmill/local

# Persistent volume data — subdirectories tracked (.gitkeep), content ignored.
# Tier-1 NFS data + tier-2 backup archives (<name>_backup) live here.
volumes/*/
# Tier-2 live databases — local only, never NFS. Same convention.
volumes_local/*/

# Tier-3 service-dir scratch (gitignored content; .gitkeep force-added)
**/code_server_data
**/comfyui_data
**/hf_cache
**/whisperx/cache
**/hermes_agent_src
**/hermes_install
**/model-cache
**/caddy_config
**/ntfy_cache
**/etc-pihole
