# --requirepass value redis-server starts with. Nextcloud authenticates against it
# with the same secret, read straight from EXIST_REDIS_PASSWORD (not this key) —
# a mismatch here throws Redis connection errors from Nextcloud's file-locking and
# session handler, not a graceful fallback.
REDIS_PASSWORD=EXIST_REDIS_PASSWORD
